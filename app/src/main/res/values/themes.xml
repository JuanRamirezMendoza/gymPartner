<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.GymPartner" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryVariant">@color/primary_dark</item>
        <item name="colorOnPrimary">@color/primary_light</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/secondary</item>
        <item name="colorSecondaryVariant">@color/secondary_dark</item>
        <item name="colorOnSecondary">@color/secondary_light</item>
    </style>

    <style name="Theme.GymPartner" parent="Base.Theme.GymPartner" />

    <!--Splash for android >= 12.-->
    <style name="Theme.GymPartner.Starting" parent="Base.Theme.GymPartner">
        <!--Set the splash screen background, animated icon, and animation duration.-->
        <item name="windowSplashScreenBackground">@color/primary</item>

        <!--Use windowSplashScreenAnimatedIcon to add either a drawable or an-->
        <!--animated drawable. One of these is required.-->
        <item name="windowSplashScreenAnimatedIcon">@drawable/little_logo</item>
        <item name="windowSplashScreenAnimationDuration">200</item>
        <!--Required for animated icons-->

        <!--Set the theme of the Activity that directly follows your splash screen.-->
        <item name="postSplashScreenTheme">@style/Theme.GymPartner</item>
        <!--Required.-->
    </style>

    <!--Splash for android < 12.-->
    <style name="Theme.GymPartner.Splash" parent="Base.Theme.GymPartner">
        <item name="android:windowBackground">@drawable/splash_bg</item>
    </style>

    <!-- Buttons -->
    <style name="PrimaryButton" parent="Widget.Material3.Button.IconButton.Filled.Tonal">
        <item name="android:textSize">12sp</item>
        <item name="textAllCaps">true</item>
        <item name="android:insetTop">0dp</item>
        <item name="android:insetBottom">0dp</item>
        <item name="android:textColor">@color/secondary</item>
        <item name="android:gravity">center</item>
        <item name="backgroundTint">@color/primary</item>
    </style>

    <style name="SecondaryButton" parent="Widget.Material3.Button.IconButton.Filled.Tonal">
        <item name="android:textSize">12sp</item>
        <item name="textAllCaps">true</item>
        <item name="android:insetTop">0dp</item>
        <item name="android:insetBottom">0dp</item>
        <item name="android:textColor">@color/primary</item>
        <item name="android:gravity">center</item>
        <item name="backgroundTint">@color/secondary</item>
    </style>

    <style name="LoginGoogleButton" parent="Widget.Material3.Button.IconButton.Filled">
        <item name="android:textSize">16sp</item>
        <item name="android:background">@drawable/btn_bg</item>
        <item name="android:textColor">@color/primary</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_width">wrap_content</item>
    </style>

    <style name="TextInformation" parent="Base.Theme.GymPartner">
        <item name="android:textSize">16sp</item>
        <item name="android:textColor">@color/secondary</item>
        <item name="android:ellipsize">none</item>
        <item name="android:maxLines">3</item>
        <item name="android:layout_marginStart">8dp</item>
        <item name="android:layout_marginEnd">8dp</item>
    </style>


    <!-- Dialog Loading Style -->
    <style name="TransparentDialog" parent="ThemeOverlay.MaterialComponents.Dialog">
        <item name="android:backgroundDimAmount">0.65</item>
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>


</resources>